import '../../styles.css';
import React from 'react'
const hello = (props) =>{
  const {open,setOpen} = props
  alert("hello",open);
  setOpen("sandip")
  console.log("Hello1",props)
}
export const Requirements = (props) =>{
  return (
      <button onClick={() => hello(props)}>login</button>
  )
} 